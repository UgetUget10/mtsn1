# Dibaca otomatis oleh `docker compose` (bukan Laravel/Next) — password MySQL untuk service db.
# Salin ke .env dan isi nilai asli. Jangan commit .env yang sudah terisi.
DB_PASSWORD=ganti-password-kuat
DB_ROOT_PASSWORD=ganti-password-root-lebih-kuat
